# 🎨 StickerSmash

一个跨平台的图片编辑应用，让用户可以在照片上添加表情贴纸，支持 iOS、Android 和 Web 平台。

## ✨ 主要功能

- 📸 **图片选择** - 从本地相库选择图片进行编辑
- 😊 **贴纸编辑** - 添加多个表情贴纸到图片上
- 🎯 **手势交互** - 支持拖拽、缩放表情贴纸
- 💾 **保存功能** - 将编辑后的图片保存到相库（移动端）或下载（Web 端）
- 🔄 **编辑控制** - 重置编辑状态，重新选择图片

## 🛠️ 技术栈

| 技术                         | 版本   | 说明            |
| ---------------------------- | ------ | --------------- |
| React                        | 18.2.0 | UI 框架         |
| React Native                 | 0.74.1 | 跨平台框架      |
| Expo                         | 51.0.5 | 开发和构建工具  |
| React Native Reanimated      | 3.10.1 | 动画库          |
| React Native Gesture Handler | 2.16.1 | 手势识别        |
| Expo Image Picker            | 15.0.4 | 图片选择器      |
| Expo Media Library           | 16.0.3 | 相库管理        |
| dom-to-image                 | 2.6.0  | 图片导出（Web） |

## 📁 项目结构

```
StickerSmash/
├── App.js                      # 主应用组件
├── components/                 # 可复用组件
│   ├── Button.js              # 主按钮组件
│   ├── CircleButton.js        # 圆形按钮组件
│   ├── IconButton.js          # 图标按钮组件
│   ├── ImageViewer.js         # 图片查看器
│   ├── EmojiPicker.js         # 表情选择器
│   ├── EmojiList.js           # 表情列表
│   └── EmojiSticker.js        # 表情贴纸组件
├── assets/                     # 资源文件
│   ├── images/                # 图片资源
│   ├── icon.png               # App 图标
│   └── splash.png             # 启动页面
├── package.json               # 依赖配置
├── app.json                   # Expo 配置
└── babel.config.js            # Babel 配置
```

## 🚀 快速开始

### 前提条件

- Node.js (v14 或更高版本)
- npm 或 yarn
- Expo CLI (可选)

### 安装依赖

```bash
npm install
# 或
yarn install
```

### 本地开发

启动开发服务器：

```bash
npm start
```

运行在特定平台：

```bash
# iOS
npm run ios

# Android
npm run android

# Web
npm run web
```

### 构建应用

使用 Expo EAS 构建：

```bash
eas build
```

## 🎯 核心功能说明

### 图片选择和编辑

用户可以从相库选择图片，应用会显示图片预览并激活编辑选项。

```javascript
// App.js 中的图片选择逻辑
const pickImageAsync = async () => {
  let result = await ImagePicker.launchImageLibraryAsync({
    allowsEditing: true,
    quality: 1,
  });
  // ...
};
```

### 贴纸管理

支持添加、拖拽和缩放表情贴纸：

- 使用 `React Native Gesture Handler` 实现手势识别
- 使用 `React Native Reanimated` 提供流畅动画
- 支持多个贴纸叠加

### 跨平台保存

- **移动端**：使用 `react-native-view-shot` 和 `expo-media-library` 保存到相库
- **Web 端**：使用 `dom-to-image` 导出为 JPEG 并下载

```javascript
// 移动端保存
const localUri = await captureRef(imageRef, {
  height: 440,
  quality: 1,
});
await MediaLibrary.saveToLibraryAsync(localUri);

// Web 端保存
const dataUrl = await DomToImage.toJpeg(imageRef.current, {
  quality: 0.95,
  width: 320,
  height: 440,
});
```

## 📱 支持的平台

- ✅ iOS
- ✅ Android
- ✅ Web

## 🔧 配置说明

### Expo 配置 (app.json)

- **App 名称**：StickerSmash
- **项目 ID**：35a1bf6b-d9e9-4db7-89a3-1a4a8733b237
- **所有者**：jouch
- **主题**：浅色模式

## 🎨 设计亮点

- 现代化的深色底部导航设计
- 平滑的过渡和动画效果
- 直观的用户界面
- 响应式布局支持多个屏幕尺寸
